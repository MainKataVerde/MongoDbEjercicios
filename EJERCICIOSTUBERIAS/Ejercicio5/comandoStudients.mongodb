use('students')

db.studentsCollection.aggregate([
    {
        $unwind: "$scores"
    },
    {
        $group: {
          _id: "$name",
          "avarageScore" : {$avg : "$scores"}
        }
    }
])