use('movies')

db.moviesCollection.aggregate([
    {
        $group: {
          _id: "$genre",
          "avarageRating" : { $avg : "$rating"},
          "count" : {$sum : 1}
        }
    }
]).sort({avarageRating : -1})